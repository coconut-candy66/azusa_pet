# ------------------------------------------------------------------
# 换行与编码策略
#
# 重要：.gitattributes 里【后出现的规则覆盖先出现的】。
# 所以通用规则必须写在最前面，特例写在最后面。
# ------------------------------------------------------------------

# 默认：文本文件，提交时归一化为 LF，检出时也用 LF
* text=auto eol=lf

# 二进制资源，明确声明以避免任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.zip binary

# ------------------------------------------------------------------
# Windows 启动器与自检说明（特例，必须放在最后）
#
# desktop/ 下的 .cmd 与 .txt 是 GBK 编码 + CRLF 换行，这是它们能被
# Windows 正确执行的必要条件。必须保持字节原样：
#   - 一旦被转成 UTF-8，中文变乱码，且 GBK 双字节会吃掉后面的 ASCII 字母，
#     命令本身被破坏；
#   - 一旦被转成 LF，cmd 解析多行 if 块失败，直接语法错退出。
# 两者的症状都是「双击后黑框一闪而过，不报任何错误」，极难排查。
#
# -text 表示「不是文本文件」：Git 不做换行归一化，也不做编码转换，
# 文件按字节原样进出仓库。
# ------------------------------------------------------------------
*.cmd -text
*.txt -text
